$kind: http-request
description: Очищает корзину
url: "{{baseUrl}}/cart/clear/"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/cart clear destroy.resources/examples
order: 1000

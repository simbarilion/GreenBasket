$kind: http-request
name: cart retrieve
description: Возвращает список товаров в корзине
url: "{{baseUrl}}/cart/"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
examples: ./.resources/cart retrieve.resources/examples
order: 5000

$kind: http-example
request:
  url: "{{baseUrl}}/schema/?format=yaml&lang=io"
  method: GET
  headers:
    - key: Accept
      value: application/vnd.oai.openapi
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    format: yaml
    lang: io
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "key_0": true
      }
order: 1000

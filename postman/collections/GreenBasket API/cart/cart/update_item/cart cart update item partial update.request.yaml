$kind: http-request
name: cart cart update item partial update
description: Обновляет количество товара в корзине
url: "{{baseUrl}}/cart/cart/update_item/"
method: PATCH
body:
  type: json
  content: "{\r

    \  \"product\": 5,\r

    \  \"quantity\": 4\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
examples: ./.resources/cart cart update item partial update.resources/examples
order: 1000
